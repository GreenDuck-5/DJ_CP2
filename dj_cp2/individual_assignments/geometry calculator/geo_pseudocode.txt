circle class
    create variables(radius, area, perimeter, diameter)
    
    print it correctly
        Radius: radius
        Area: area
        Perimeter: perimeter
        Diameter: diameter

circle creation function
    ask user for radius of circle
    diameter = radius * 2
    perimeter = 2 * pi * radius
    area = pi * radius ** 2

    circle class(radius, area, perimeter, diameter)



rectangle class
    create variables(height, width, perimeter, area)
    
    print it correctly
        Height: height
        Width: width
        Perimeter: perimeter
        Area: area

rectangle creation function
    ask user for the height and width
    perimeter = (height * 2) + (width * 2)
    area = height * width

    rectangle class(height, width, perimeter, area)



square class
    create variables(height, perimeter, area)

    print it correctly
        Height: height
        Width: height
        Perimeter: perimeter
        Area: area

square creation function
    ask user for height
    perimeter = (height * 2) + (width * 2)
    area = height * width

    rectangle class(height, height, perimeter, area)



triangle class
    create variables(height, width, perimeter, area)
    